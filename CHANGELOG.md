# 更新日志（Changelog）

本文件记录「图片嵌字工具」各版本的功能变更。版本号按 `v主版本.次版本.修订` 递增。

---

## [v2.6.4] - 2026-09-07

### 变更
- **内置字体**：方正卡通简体文件由 `方正卡通简体.ttf` 替换为 `方正卡通_GBK.ttf`（族名仍为「方正卡通简体」）

---

## [v2.6.3] - 2026-09-07

### 改进
- **滚动条**：深色/浅色主题统一为细滑块、透明轨道，降低两侧栏与面板滚动条的突兀感

---

## [v2.6.2] - 2026-09-07

### 新增
- **角度数值**：旋转面板显示当前角度，支持输入与 ± 步进细调（Shift+点击步进 5°）；旋转模式中改数字只更新预览 draft
- **样式预设含旋转**：保存/套用样式时包含 `rotation`；旧预设缺字段视为 0°；竖排套用时角度强制为 0

---

## [v2.6.1] - 2026-09-07

### 修复
- **确认旋转**：点击 ✓ 时不再被画布清空选中误触发 abort，角度可正确固定
- **✓ / × 布局**：改到文字框下方水平并列，避免矮框时重叠
- **旋转面板按钮**：改为实心 `.btn` 样式，更易识别可点击

---

## [v2.6.0] - 2026-09-07

### 新增
- **文字旋转模式**：选中横排文字后点「开始旋转」，拖动文字框绕中心旋转（±180°）
- 松手后屏幕坐标显示 ✓ / ×：确认角度并退出，或还原进入旋转模式前的角度并继续调整
- 「退出旋转模式」、Esc、切画图/换图/换选等会取消未确认角度并退出；竖排禁用旋转
- 导出 Canvas 与预览角度一致；角度随工程写入 IndexedDB

---

## [v2.5.5] - 2026-09-02

### 文档
- **README**：添加在线演示地址 [simple-comic-text-embedding-tool.pages.dev](https://simple-comic-text-embedding-tool.pages.dev/)

---

## [v2.5.4] - 2026-09-02

### 修复
- **Cloudflare 部署**：移除 `_redirects`（`/ /index.html 200` 在已有 `index.html` 时会触发无限循环，报错 code 100324）
- 新增 `.wranglerignore`，Wrangler CLI 部署时排除 `.git` 目录

---

## [v2.5.3] - 2026-09-02

### 部署
- 新增 `index.html`（Web 入口，与 `双击我开始嵌字.html` 内容相同）
- 新增 `_redirects`（`/ /index.html 200`），适配 Cloudflare Pages 根域名访问
- 新增 `.gitignore`，初始化 Git 仓库

---

## [v2.5.2] - 2026-09-02

### 文档
- **README**：入口文件名改为 `双击我开始嵌字.html`；内置字体数量与 `BUNDLED_FONTS` 对齐（3 套 / 4 文件）；补充上传字体 IndexedDB 持久化说明
- **帮助弹窗**：移除「上传字体仅本次打开有效」的过时描述，与 v2.1.0 起的持久化行为一致

---

## [v2.5.1] - 2026-09-01

### 改进
- **文字取色器**：嵌字模式属性面板新增取色器，可从图片上吸取文字颜色与描边颜色（与画图取色器一致）
- **拖动与编辑分离**：文字区内单击即可编辑、拖选、定位光标；移动文字框请拖顶部条或左/右/底边距条
- **八点缩放**（横排）：逻辑不变，与专用拖动区互不干扰

---

## [v2.5.0] - 2026-09-01

### 改进
- **文本框折行**：横排文字用 `wrapText` 计算自动折行后的高度，长句不再被裁切
- **宽度模式**：`auto` 随内容适配宽度；手动拖水平手柄后锁定为 `fixed`，仅高度随折行变化
- **八点缩放**（横排）：八点均可缩放——左右/四角改宽度，上下/四角改高度；字号不变
- **导出对齐**：折行宽度与编辑态 inner 宽度一致

---

## [v2.4.0] - 2026-09-01

### 改进
- **文本框拖动**：编辑时可拖出图片区域，导出仍只保留图片范围内的文字（canvas 自然裁切）
- **文本框测宽**：横排文字改用 Canvas 按行测宽 + 字体预加载，框宽贴合最长行，减少左右空白
- **嵌字撤销/重做**：嵌字模式支持 `Ctrl+Z` / `Ctrl+Y` 撤销文字操作
- **图形预设**：插入预设图形后自动切换到对应矩形/椭圆工具，便于立即拖动调整

---

### 改进
- **预设交互改版**：样式预设与图形预设统一为「点空白槽保存 · 点有内容应用/插入 · × 删除」，移除右键操作
- **样式预设**：独立 2×2 大格，固定样例字故/伦/德/里 预览完整样式（含字体、加粗、竖排、描边）
- 样式预设存储扩展 `fontFamily`、`bold`、`vertical`

---

## [v2.2.0] - 2026-08-31

### 新增
- **图形预设**：画图模式下 6 个独立槽，右键保存选中矩形/椭圆（类型、比例、颜色），左键在图片中心插入相同图形
- 预设槽内 SVG 迷你预览（棋盘格底），刷新后保留（localStorage）

---

## [v2.1.0] - 2026-08-31

### 新增
- **工程持久化**：使用 IndexedDB 自动保存图片、嵌字/画图编辑、上传字体与会话状态，刷新后自动恢复
- 顶栏「清除缓存」按钮，可一键 wipe 本地 IndexedDB 数据

### 改进
- 「清空列表」同步清除 IndexedDB 中的工程数据
- 页面隐藏时 flush 未保存的编辑（debounce 补写）

---

## [v2.0.0] - 2026-08-31

### 新增
- **三栏现代化界面**：左侧图库 | 中间画布 | 右侧属性面板（Figma 风格布局）
- **深色 / 浅色主题切换**：顶栏一键切换，偏好写入 localStorage
- **侧栏折叠**：左右侧栏均可折叠，状态持久化
- **帮助弹窗**：首次访问自动引导，按 `?` 随时打开
- **空状态拖拽区**：画布区虚线 dropzone，拖拽时全屏高亮反馈
- **默认样式编辑**：未选中文字框时，右侧面板可编辑「新文字框默认样式」

### 改进
- 工具栏从画布浮动改为右侧固定属性面板，不再遮挡图片
- SVG 图标替换 Emoji，统一视觉风格
- CSS 模块化拆分（tokens / theme / layout / components）
- Toast 通知移至右上角

### 变更
- 「导出所选」按钮移至左侧图库底部
- 「嵌字 / 画图」模式切换移至顶栏中央

---

## [v1.7.0] - 2026-08-15

### ✨ 新增
- **样式预设升级**：4 个预设槽由「纯颜色」升级为「整组样式」，一键保存 / 套用 **颜色、字号、描边开关、描边颜色、描边粗细**（不包含竖排开关、字体样式）
- 兼容旧版纯颜色预设数据（自动迁移，不丢失）

---

## [v1.6.0] - 2026-08-15

### ✨ 新增
- **颜色预设**：4 个预设色块（左键应用、右键把当前颜色存入），localStorage 持久化

---

## [v1.5.0] - 2026-08-15

### ✨ 新增
- **样式持久化**：最近使用的文字样式（颜色 / 字号 / 字体 / 竖排 / 描边）保存到 localStorage，刷新 / 关闭页面后仍保留

---

## [v1.4.0] - 2026-08-15

### ✨ 新增
- **文字描边**：描边开关、描边颜色、描边粗细（随字号比例缩放），预览与导出效果一致，竖排同样支持

---

## [v1.3.0] - 2026-08-15

### 🚀 改进
- **样式记忆**：切换图片后，新增文字框自动沿用最近一次使用的颜色 / 字号 / 字体 / 竖排，无需重复设置

---

## [v1.2.0] - 2026-08-15

### ✨ 新增
- **一键清空列表**：图库栏「清空」按钮，一键移除全部已上传图片（带确认提示）

### 🔄 变更
- **导出重命名**：导出图片自动命名为「原文件名**已嵌字**.png」，重名自动追加 `(2)`、`(3)` 序号

---

## [v1.1.0] - 2026-08-15

### ✨ 新增
- **文字竖排**：支持竖版排列（从左到右），Enter 另起一列
- **内置 5 套字体**：自动加载「字体样式」目录（Aa剑豪体、乐米元气团团体、寒蝉半圆体、文渊圆体 ×6 字重、黑糖话梅）
- **上传自定义字体**：支持 `ttf / otf / woff / woff2` 与 **zip**（内置解压自动查找字体文件）

---

## [v1.0.0] - 2026-08-15

### 🎉 初始版本
- 批量上传图片（多选 / 拖拽）
- 图片任意位置添加文字框：自由输入文本、可拖动
- 每个文字框可独立设置文字颜色、字体大小
- 勾选图片一键导出全部为 ZIP（内置 ZIP 生成器，零依赖）
- 单张导出 PNG（按原图分辨率渲染）
